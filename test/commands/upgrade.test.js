const {expect, test} = require('@oclif/test')

describe('upgrade', () => {
  // test
  // .stdout()
  // .command(['upgrade'])
  // .it('runs hello', ctx => {
  //   expect(ctx.stdout).to.contain('hello world')
  // })
})
